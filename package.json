{
  "scripts": {
    "watch": "watchify lib/main.js -o public/bundle.js -dv",
    "dev": "npm run watch & ecstatic public",
    "build": "browserify lib/main.js > public/bundle.js && cp style/main.css public/style.css",
    "deploy": "git subtree push --prefix public origin master"
  },
  "dependencies": {
    "catch-links": "^2.0.0",
    "routes": "^2.1.0",
    "single-page": "^1.1.0"
  },
  "devDependencies": {
    "ecstatic": "^1.4.0"
  }
}
